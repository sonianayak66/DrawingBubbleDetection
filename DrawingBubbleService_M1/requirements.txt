# ─────────────────────────────────────────────────────────────────────────────
# Production dependencies — Bubble / Dimension Detector
#
# Python version : 3.12.6
# Environment    : Fully offline (no internet access at runtime)
# Tested on      : Windows 10/11, Ubuntu 22.04
#
# All packages must be installed from wheels downloaded in advance.
# Download command (run once on a machine with internet access):
#
#   pip download -r requirements.txt -d ./wheels
#
# Install offline:
#
#   pip install --no-index --find-links=./wheels -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── Computer Vision ───────────────────────────────────────────────────────────
opencv-python==4.10.0.84

# ── Numerical / Scientific ────────────────────────────────────────────────────
numpy==1.26.4
scipy==1.11.4

# ── OCR — primary engine (CPU-only) ──────────────────────────────────────────
# IMPORTANT: This stack is CPU-only. Do NOT install onnxruntime-gpu —
# it pulls in ~200 MB of CUDA libraries and is unused.
onnxruntime==1.17.0
rapidocr_onnxruntime>=1.3.2,<2.0

# ── Web API ───────────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
python-dotenv==1.0.1
python-multipart==0.0.9

# ── System / Monitoring ──────────────────────────────────────────────────────
psutil==6.0.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.3.3
httpx==0.27.2

# ─────────────────────────────────────────────────────────────────────────────
# OPTIONAL — Additional OCR engines (improve accuracy, not required to run)
#
# PaddleOCR v5 — better small-text detection:
#   pip install paddlepaddle==3.0.0 paddleocr==3.4.0
#
# Florence-2 — tiny-text reading via Microsoft's vision model:
#   pip install torch==2.4.0 torchvision==0.19.0 transformers==4.44.0
#
# VLM verification — local vision model for post-assignment correction:
#   pip install ollama
#   Then: ollama pull minicpm-v
#   Enable with: enable_vlm_verification=True in DetectionConfig
#
# GPU acceleration is INTENTIONALLY DISABLED in this deployment.
# The detector code passes det_use_cuda=False / cls_use_cuda=False /
# rec_use_cuda=False to RapidOCR. If you still see CUDA mentioned in
# logs, uninstall onnxruntime-gpu and reinstall the CPU build:
#   pip uninstall -y onnxruntime-gpu
#   pip install onnxruntime==1.17.0
# ─────────────────────────────────────────────────────────────────────────────
